# 소프트웨어 요구사항 명세서 (SRD)

**프로젝트명:** Fast WebP Converter (가칭)
**버전:** 1.0
**작성일:** 2025-12-29

## 1. 프로젝트 개요

* **목표:** 사용자가 지정한 폴더 내의 대량의 이미지를 UI 프리징(멈춤) 없이 빠르게 미리보고, 선택적으로 WebP 포맷으로 변환하여 저장하는 데스크톱 애플리케이션 개발.
* **핵심 제약 사항:**
* **UI 응답성:** 수백 장의 이미지를 로드하더라도 스크롤이나 클릭 반응이 즉각적이어야 함.
* **동시성:** 이미지 변환 작업 중에도 앱이 멈추지 않고 진행률을 실시간으로 표시해야 함.



## 2. 기술 스택 및 환경

* **언어:** Python 3.10 이상
* **GUI 프레임워크:** **PyQt6 (또는 PySide6)**
* *선정 이유:* `Tkinter`는 이미지 렌더링 성능이 낮아 대량의 썸네일 그리드 뷰 구현 시 스크롤 버벅임이 발생함. GPU 가속 및 모델/뷰 아키텍처 지원이 강력한 Qt 프레임워크가 필수.


* **이미지 프로세싱:** **Pillow (PIL)**
* **병렬 처리:** `multiprocessing` (변환용), `QThread` (UI 렌더링용)

---

## 3. 기능 요구사항 (Functional Requirements)

### 3.1 파일 탐색 및 로딩 (Loading)

* **[REQ-L-01] 소스 폴더 선택:** 사용자는 시스템 다이얼로그를 통해 폴더를 선택한다. 하위 폴더 포함 여부는 **포함하지 않음**을 기본으로 한다 (1 depth).
* **[REQ-L-02] 필터링:** 선택된 폴더에서 다음 확장자를 가진 파일만 로드한다: `jpg`, `jpeg`, `png`, `bmp`, `tiff`.
* **제외 조건:** 확장자가 `.webp`인 파일은 목록 로딩에서 원천 배제한다.


* **[REQ-L-03] 메타데이터 스캔:** 파일의 전체 데이터를 읽지 않고, 파일명과 경로만 우선 확보하여 UI 리스트를 즉시 초기화한다.

### 3.2 이미지 뷰어 및 성능 최적화 (Viewing & Performance)

* **[REQ-V-01] 비동기 썸네일 생성:**
* 메인 스레드(UI)는 빈 사각형(Placeholder)을 먼저 그린다.
* 백그라운드 스레드(`QThread`)에서 원본 이미지를 읽어 **높이 150px~200px 수준의 썸네일**로 리사이징하여 메모리에 캐싱한다.
* 생성이 완료되는 즉시 UI의 해당 위치 이미지를 교체한다.


* **[REQ-V-02] 그리드 레이아웃:** 이미지는 바둑판식(Grid) 배열로 표시되며, 창 크기에 따라 열 개수가 반응형으로 조절되어야 한다.
* **[REQ-V-03] 파일명 표시:** 각 썸네일 하단에 파일명을 표시하되, 너무 길 경우 말줄임표(...) 처리한다.

### 3.3 선택 및 상호작용 (Selection)

* **[REQ-S-01] 개별 선택:** 이미지 또는 체크박스를 클릭하여 변환 대상을 선택/해제한다 (토글).
* **[REQ-S-02] 일괄 제어:**
* '전체 선택' 버튼: 로드된 모든 이미지 선택.
* '전체 해제' 버튼: 선택 초기화.


* **[REQ-S-03] 상태 표시:** 하단 상태바에 `총 N개 파일 중 M개 선택됨`을 실시간으로 표시한다.

### 3.4 변환 엔진 (Conversion Engine)

* **[REQ-C-01] 포맷 변환:** 선택된 이미지를 WebP 포맷으로 변환한다.
* **[REQ-C-02] 품질 설정 (Hard Requirement):**
* 사용자가 품질(Quality)을 제어할 수 있어야 함 (범위: 1~100).
* **기본값:** 80 (Lossy 압축, 육안상 화질 저하가 적고 용량 감소 효율이 좋음).


* **[REQ-C-03] 병렬 처리:** `multiprocessing.Pool`을 사용하여 CPU 코어 수만큼 병렬로 변환 작업을 수행하여 속도를 극대화한다.

### 3.5 저장 및 파일명 처리 (Saving & Naming)

* **[REQ-F-01] 저장 경로 지정:**
* **Default:** 원본 파일이 있는 폴더에 저장.
* **Custom:** 사용자가 별도의 '출력 폴더'를 지정 가능.


* **[REQ-F-02] 중복 파일명 처리 알고리즘 (필수):**
* 저장하려는 경로에 `image.webp`가 이미 존재할 경우 덮어쓰지 않는다.
* 파일명 뒤에 순차적 번호를 부여한다.
* *Logic:* `image(1).webp` 존재? -> Yes -> `image(2).webp` 확인 ... -> 없는 번호 확정 시 저장.



### 3.6 진행 표시 (Feedback)

* **[REQ-P-01] 프로그레스 바:** 변환 시작 시 팝업 또는 UI 하단에 진행률(0% ~ 100%)을 표시한다.
* **[REQ-P-02] 작업 차단:** 변환이 진행되는 동안에는 폴더 변경이나 이미지 선택 변경 등 충돌을 야기하는 UI 조작을 비활성화(Disable) 한다.
* **[REQ-P-03] 결과 리포트:** 작업 완료 시 `성공: N건, 실패: M건`을 알리는 메시지 박스를 띄운다.

---

## 4. 비기능 요구사항 (Non-functional Requirements)

* **NF-01 (반응성):** 이미지 1,000장이 있는 폴더를 로드할 때 앱이 멈춘 것처럼 보여선 안 된다(Spinning wheel 금지).
* **NF-02 (안정성):** 손상된 이미지 파일을 읽더라도 앱이 강제 종료(Crash)되지 않고, 해당 파일만 '실패' 처리 후 다음 작업으로 넘어가야 한다.
* **NF-03 (자원 관리):** 썸네일 생성 시 원본 이미지는 메모리에서 즉시 해제하여 RAM 누수를 방지한다.

---

## 5. 예외 처리 계획 (Exception Handling)

| 시나리오 | 처리 방안 |
| --- | --- |
| **읽기 권한 없음** | 해당 파일을 리스트에서 붉은색 표시 혹은 로드 시 건너뛰고 에러 로그 기록. |
| **이미지 데이터 손상** | `UnidentifiedImageError` 포착 시 변환 실패 카운트를 올리고 계속 진행. |
| **디스크 용량 부족** | 변환 즉시 중단 및 사용자에게 경고 메시지 출력. |
| **사용자가 작업 중 강제 종료** | 진행 중인 서브 프로세스를 안전하게 terminate 시키는 종료 로직 구현 (`atexit` 등 활용). |

---

## 6. 개발 로드맵 (제안)

이 SRD에 기반하여 개발은 다음 순서로 진행되어야 논리적입니다.

1. **Phase 1 (Skeleton):** PyQt6 기본 UI 구성 + 폴더 선택 기능 + 파일 리스트 출력 (이미지 없이 텍스트만).
2. **Phase 2 (Async Loader):** QThread를 이용한 비동기 썸네일 로더 구현 및 그리드 뷰 연동 (여기서 성능 검증 필수).
3. **Phase 3 (Converter):** Multiprocessing을 이용한 WebP 변환 로직 구현 (중복 이름 처리 포함).
4. **Phase 4 (Integration):** UI와 변환 엔진 연결, 프로그레스 바 구현, 예외 처리.